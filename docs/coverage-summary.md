# Test Coverage Breakdown
## EntreSkill Hub

| | |
|---|---|
| **Version** | 1.0 |
| **Generated by** | `npm run test:unit -- --coverage` + `npm run test:integration -- --coverage` |
| **Companion Docs** | Testing.md |

> This document is regenerated by CI on every run against `main` and committed back via an automated PR, or published as a CI artifact if the team prefers not to commit generated reports.

---

## 1. Overall Coverage Summary

| Metric | Coverage | Threshold | Status |
|---|---|---|---|
| Statements | —% | 75% | ☐ |
| Branches | —% | 70% | ☐ |
| Functions | —% | 75% | ☐ |
| Lines | —% | 75% | ☐ |

---

## 2. Coverage by Domain Module

| Domain | Statements | Branches | Functions | Lines | Threshold | Notes |
|---|---|---|---|---|---|---|
| `domains/auth` | —% | —% | —% | —% | 80% (elevated) | Session/RBAC-critical |
| `domains/assessment` | —% | —% | —% | —% | 75% | |
| `domains/recommendations` | —% | —% | —% | —% | 90% (elevated) | Core scoring logic — highest bar |
| `domains/roadmaps` | —% | —% | —% | —% | 85% (elevated) | Progress calculation correctness |
| `domains/resources` | —% | —% | —% | —% | 75% | |
| `domains/bookmarks` | —% | —% | —% | —% | 70% | Low complexity |
| `domains/mentors` | —% | —% | —% | —% | 80% | Verification + session logic |
| `domains/notifications` | —% | —% | —% | —% | 70% | |
| `domains/admin` | —% | —% | —% | —% | 85% (elevated) | High-trust operations, audit logging |
| `lib/auth.ts` | —% | —% | —% | —% | 90% (elevated) | |
| `lib/rate-limit.ts` | —% | —% | —% | —% | 80% | |
| `lib/audit-log.ts` | —% | —% | —% | —% | 85% | |
| `components/*` (shared UI) | —% | —% | —% | —% | 60% | Presentational — lower bar by design |

---

## 3. Uncovered Critical Paths (Tracked, Not Yet Tested)

Maintain this list actively — anything here is a known gap, not an oversight to be discovered later.

| Path | Domain | Risk if untested | Owner | Target sprint |
|---|---|---|---|---|
| — | — | — | — | — |

---

## 4. E2E Coverage (Critical User Journeys)

Playwright E2E does not report line coverage in the traditional sense — tracked instead as journey coverage against the list defined in Testing.md §5.

| Journey | Automated? | Last verified |
|---|---|---|
| Registration → assessment → recommendations → roadmap start → step completion | ☐ | — |
| OAuth sign-in → assessment | ☐ | — |
| Bookmark idea → view in Saved | ☐ | — |
| Mentor application → verification → public discovery | ☐ | — |
| Mentor session request → confirm → Q&A → completion → feedback | ☐ | — |
| Admin content approval (mentor upload → pending queue → approve → public library) | ☐ | — |
| Admin business idea/roadmap curation → appears in recommendations | ☐ | — |
| Admin RBAC enforcement (non-admin blocked from `/admin/*`) | ☐ | — |

---

## 5. Coverage Trend

| Date / Release | Statements | Branches | Functions | Lines |
|---|---|---|---|---|
| — | —% | —% | —% | —% |

Track this table release-over-release to catch coverage erosion early rather than discovering it during an incident postmortem.

---

## 6. How to Regenerate This Report Locally

```bash
npm run test:unit -- --coverage
npm run test:integration -- --coverage
npm run coverage:merge   # merges unit + integration lcov reports
npm run coverage:report  # outputs this file's data tables
```

# Fix Plan — Build, TypeScript, ESLint & Dependency Cleanup

## Steps

- [x] 1. Analyze the codebase and understand all issues
- [x] 2. Fix notifications/page.tsx — Simplify grouping logic (TypeScript build fix)
- [x] 3. Fix use-autosave.ts — Remove unused generic `T`
- [x] 4. Fix use-unsaved-changes.ts — Remove unused `router`
- [x] 5. Fix ideas/page.tsx — Replace `<img>` with `<Image>` from next/image
- [x] 6. Fix company-logos.tsx — Replace `<img>` with `<Image>` from next/image
- [x] 7. Fix eslint.config.mjs — Add coverage/, lcov-report/, dist/ to ignores
- [x] 8. Fix package.json — Add `dotenv` to devDependencies
- [x] 9. Run typecheck, lint, and build to validate
- [x] 10. Deliver final report

